#pragma once
#include "Framebuffer.hpp"
#include "Vec2.hpp"

#include <glad/glad.h>
#include <memory>
#include <shaderdirect.hpp>

// Attachment

// A attach to B

// Changes in B propagate to A 

// Directed graph implementation (Linked list, Tree)

// Linked list good for one dimensional hierarchies

// Tree good for higher dimensional hierarchies


// GUI model:

// Client sends commands to the server
// Server manages and processes commands, internally manages object model
// Server issues rendering commands to the rendering engine

// Object model:

// 